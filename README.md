# sudoku-rl

This repo explores reinforcement learning techniques and fine-tuning methods for helping small language models get better at playing Sudoku.

The goal is to use Sudoku as a compact testbed for studying reasoning, search, feedback, and policy improvement in small LLMs.

## Conventions

The sudoku is represented by a string of 81 numbers. Missing fields are marked as `.`.
